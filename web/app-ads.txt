# app-ads.txt — AdMob yetkili satıcı beyanı (IAB "Authorized Sellers for Apps")
#
# NE İŞE YARIYOR: reklam alıcıları, bu uygulamanın envanterini kimin
# satabildiğini uygulamanın MAĞAZA KAYDINDAKİ geliştirici web sitesinden
# (`https://kimo.work/app-ads.txt`) okuyor. Dosya yoksa ya da yayıncı
# kimliği tutmuyorsa AdMob doğrulaması BAŞARISIZ olur ve doluluk düşer.
#
# NEREYE KONACAK: alan adının KÖK dizinine, tam olarak `/app-ads.txt` yolunda,
# `text/plain` olarak. Mağaza kayıtlarındaki geliştirici sitesi de aynı alan
# adı olmalı (App Store Connect → "Marketing URL"/"Support URL",
# Play Console → "Store listing → Website"). AdMob taraması yayından sonra
# birkaç gün sürebilir.
#
# YAYINCI KİMLİĞİ (Task 18): `pub-3524308481275678`, yayın AdMob uygulama
# kimliğinden türetildi (`ios/Flutter/Release.xcconfig`:
# `ca-app-pub-3524308481275678~…` — `ca-app-pub-` ile `~` arasındaki 16 hane
# hesabın yayıncı kimliğidir). CI kapısı (`ci.yml` → "app-ads.txt") bu iki
# yerin AYNI sayıyı taşımasını zorluyor; yer tutucu `pub-0000000000000000`
# görürse kırmızı düşüyor.
#
# SAHİP TEYİDİ: AdMob konsolu → Settings → Account information → Publisher ID
# ile karşılaştırın. Yanlış bir kimlikle yayınlamak boş dosyadan kötüdür.
#
# Biçim:  google.com, pub-<YAYINCI-KIMLIGI>, DIRECT, f08c47fec0942fa0
#         └ reklam sistemi  └ hesap kimliği   └ doğrudan  └ TAG-ID (Google'ın
#                                               satış       sertifikasyon
#                                               yetkisi     kimliği, sabit)

google.com, pub-3524308481275678, DIRECT, f08c47fec0942fa0
